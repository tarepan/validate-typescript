{
  "name": "validate-typescript-test",
  "version": "1.0.0",
  "description": "Testing validate typescript",
  "main": "out/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf out",
    "setup": "./setup.sh",
    "test": "npm run build && ava out --verbose && npm run clean"
  },
  "author": "Grant Zietsman",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "ava": "^0.25.0"
  }
}
